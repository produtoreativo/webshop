# Context Capsule

Localização canônica: gerado pelo gate de readiness do `/downstream` em
`prodops/artifacts/iterations/<iteration-id>/cards/<card-slug>/context.md`.
Capsules são geradas, não escritas à mão — regenerar via `/downstream`, nunca editar diretamente.

A capsule é o único artefato que o agente precisa carregar para executar o flow completo
de CI Sync e CI Async sem reler arquivos de infraestrutura. Campos marcados como
`(preenchido por <fase>)` são deixados em branco na geração e preenchidos em runtime.

---

# Card: <slug>

(generated by /downstream readiness — do not edit by hand)

Date: <YYYY-MM-DD>

## Runtime Context

Dados de execução propagados para todas as fases — Bootstrap, Hack, Sync, Finish, Ship,
Validate, Promote. O agente não precisa reler iteration-plan.md nem plan.md durante o flow.

```
ds-id:              DS-<n>
work-item-id:       <issue-number-da-iteração-corrente>   ← resolve DS-ID → issue via plan.md
iteration-id:       <version>                              ← ex: v0.6.0
iteration-dir:      prodops/artifacts/iterations/<version>/
correlation-id:     <uuid-gerado-em-Delivery.Plan.Bootstrap.Issue.Entered>
actor-player:       <claude|codex|copilot>
```

## Runtime Paths

Paths pré-computados para eliminar derivação manual em cada fase.

```
feature-branch:       feat/<work-item-id>-<slug>
base-branch:          master
timeline-path:        prodops/artifacts/iterations/<version>/runtime/timelines/<work-item-id>.json
plan-bootstrap-path:  prodops/artifacts/iterations/<version>/runtime/plan-bootstrap.json
plan-validate-path:   prodops/artifacts/iterations/<version>/runtime/plan-validate.json
session-trail-dir:    prodops/artifacts/trails/sessions/   ← fixo; nunca escopado por iteração
obc-path:             prodops/artifacts/obcs/<capability>.md
bdd-path:             prodops/artifacts/bdd/<capability>.feature
reliability-path:     prodops/artifacts/plans/reliability/<capability>.md   ← ou "none" se não existir (opcional)
```

## Flow State

Campos preenchidos progressivamente durante o flow. Iniciam em branco.

```
pr-number:      (preenchido pelo Finish)
infra-scope:    (preenchido pelo Ship — dynamo|lambda|both|none)
oem-state:      PENDING
```

## OBC

Path: `prodops/artifacts/obcs/<capability>.md`

Success criteria (verbatim — the 3–6 measurable lines from the OBC):

- <criterion 1>
- <criterion 2>
- <criterion 3>

## BDD

Path: `prodops/artifacts/bdd/<capability>.feature`

Scenarios (full — Given/When/Then para uso direto no Red phase do Hack/tdd):

```gherkin
Scenario: <title>
  Given <precondition>
  When <action>
  Then <outcome>

Scenario: <title>
  ...
```

## Risks

Entradas relevantes para este card, filtradas de `prodops/artifacts/risks/risks.md`:

- `<RISK-ID>` — <one-line description and mitigation status>

## Reliability (opcional)

SLOs, timeouts e requisitos de idempotência para este card.
Preencher se `reliability-path != "none"`, omitir caso contrário.

- <SLO / timeout / idempotency requirement>

## Commands

Lidos de `prodops/exec/manifest.yaml` (seção `gates`). Preencher todos os gates aplicáveis.

- `lint`:       <command> — expect: exit 0
- `build`:      <command> — expect: exit 0
- `acceptance`: <command> — expect: exit 0
- `no_mocks`:   <command> — expect: zero hits
- `smoke`:      <command ou "Bootstrap gate only">

## Dependencies

Issues que devem estar merged antes do Bootstrap deste card:

- <DS-ID> → #<issue> — <motivo> (ou "None")

## Open questions

Qualquer item pendente. O agente NÃO deve resolver silenciosamente — parar e reportar.

- <pending question, ou "None">
