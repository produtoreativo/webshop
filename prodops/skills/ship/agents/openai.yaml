interface:
  display_name: "SHIP"
  short_description: "Prepare TDD-backed PR submissions"
  default_prompt: "Use $ship to verify TDD evidence, run final security and quality checks, summarize the changes, and prepare or submit a pull request."
policy:
  allow_implicit_invocation: true
